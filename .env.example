# Azure SQL Server Database Configuration
# Copy this file to .env and fill in your actual values

# Database Name (from Azure: portfolio/portfolio)
DB_NAME=portfolio

# Database Server Host (Azure SQL Server FQDN)
# Format: <server-name>.database.windows.net
DB_HOST=your-server-name.database.windows.net

# Database User (SQL Server Authentication)
DB_USER=your-admin-username

# Database Password (SQL Server Authentication)
DB_PASSWORD=your-admin-password

# Database Port (default for SQL Server)
DB_PORT=1433

# Optional: Django Secret Key for production
SECRET_KEY=your-production-secret-key

# Optional: Debug mode (set to False in production)
DEBUG=False

# Optional: Allowed hosts for production
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com